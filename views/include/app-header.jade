header#header
    a#logo(href='/') 平台项目进度表
    .search.dark-fields
        form(action='/')
            input(type='search', name='keyword', autocomplete='off')
    - if (me !== null)
        a.user-avatar(href='/users/#{me._id}')
            span  #{me.name}
            img(src='#{me.avatar_url}')